DATABASE_CONFIG.txt contains all MYSQL instruction for the creation of the database.

You have to replace the address of the site in config.php, replace YOURSALT by a proper salt, and replace YOURPASSWORD in config.php and DATABASE_CONFIG.txt by the password of your choice.

Afterwards, simply copy/paste all the mysql instruction in DATABASE_CONFIG.txt.
